version: '3'

services:
  redis:
    image: redis:latest

  stdweb:
    build: .
    ports:
      - "8123:8000"
    volumes:
      - ./data:/opt/stdweb/data/
      - ./tasks:/opt/stdweb/tasks/
      - ./index-data:/usr/local/astrometry/data
    command: >
      /usr/bin/env bash
